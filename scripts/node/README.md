# TypeScript scripts

[Source](https://tsx.is/shell-scripts)

## Adding a script

1. Add the tsx shebang to the start of new the new script: `#!/usr/bin/env -S pnpm tsx`
2. Make the file executable: `chmod +x ./scripts/node/file.ts`
3. Now, you can run `file.ts` directly: `./scripts/node/file.ts`
